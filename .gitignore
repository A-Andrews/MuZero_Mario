__pycache__/
*.pyc
*.pyo
*.egg-info/
.pytest_cache/
.venv/
venv/
/env/

# Outputs
outputs/
# Anchored: a bare `logs/` also matched src/logs/, which silently kept the
# logging package out of git and lost it in the BMRC -> Isambard migration.
/logs/
wandb/
wandb_runs/
multirun/

# Hydra
.hydra/

# Checkpoints and videos
*.pt
*.mp4
*.ckpt
*.log

# ROM / state files (symlinked, never commit)
mario.stimuli/
mario.stimuli

# OS
.DS_Store
